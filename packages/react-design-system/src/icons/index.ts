// e& App Icons — folded into the design system. `Icon` is the name-based renderer
// (<Icon name="wallet" />); named components (WalletIcon…), the ICONS registry, and
// ICON_META (descriptions + search aliases) are also exported.
export * from './icons';
export * from './metadata';
