# V1.1 Audit — 09 Product-specific

- **Section:** 09-product-specific
- **Audit date:** 2026-07-06
- **Figma file:** `pzm63BTLfPfT1stcF89ILQ` — "e& Consumer App DSL V1.1" (published library)
- **Pages audited:** Smiles Balance `26610:601` (page name carries **🟣** flag) · Voucher `28278:13069` (page name carries **🔵** flag)
- **Code:** `packages/react-design-system/src/components/cards.tsx` (`SmilesBalance`, `Voucher`)

## Drift table

| Component | Figma node (V1.1) | Flag | Figma spec (variants/dims) | Code (export/props) | Anatomy file | Verdict | Action |
|---|---|---|---|---|---|---|---|
| Smiles Balance | final set `smiles-balance-card` **27131:101113** (`states=points-activated` 27131:101112 · `states=points-not-active` 27131:101114, both 353×148); core `.smiles-balance-card-core` **26610:3492** / instance 27131:101087; atom set `.next-tier` **26663:89987** (silver 26663:89986 67×18 · gold 26663:89988 60×18 · plat 26663:89990 88×18); template = section slot: card 335w + shared `action-bar` 72h | 🟣 page | `states`(points-activated/not-active), 353×148 (335 in section slot); white card `color/surface/base/inverse`, r `border-radius/5` **16**, clip; header pt/px `lg` 16: points+"Points" `heading/lg` Bold **28** lh1.2 tr −0.65 `text/action/midnight/default` #191329 + tier pill (bg `bg/component/light-gray` #f4f3f5, pill r100, pl4 pr8 py4, 32px Smiles-loyalty medallion + 12px Book label `text/default/dark/low-emphasis` .5); footer px `lg` pt `sm` 8 gap4: `.progress-bar-core` h12 track `surface/sunken/inverse` #f7f7fa bar `text/positive/subtle` #54bc72 r1200 + line `body/md` 14 `text/default/dark/high-emphasis` #140f21 "Just XXXX points to your" + `.next-tier` gradient-text `title/xs` SemiBold 14 (Gradients (NEW)/Gold radial #e2c668→#cca951→#b78d3b; silver/plat variants); not-active: "0 Points", bar 0, "Get XXXX points for activating", no tier pill/next-tier; CTA lives OUTSIDE card as shared `action-bar` h72 in template | `SmilesBalance` — props `points`/`cta` only; **gold-gradient** bg linear-gradient(120deg,#e2c668,#d9b14a) (V1.0 tier-themed residue ≈ V1.1 gold *text* gradient), r16, p `lg`, horizontal row: "Smiles balance" `body.sm` rgba(25,19,41,.7) + points `heading.sm` 20 + `Button sm secondary` "Redeem" inside card; no states axis, no progress bar, no tier pill, no next-tier line | `smiles-balance.md` — variants `next-tier`(gold/plat/silver) × `states`(points-activated/not-active) ✓, 353×148 + atom dims ✓, r16 ✓, header · footer ✓ (matches V1.1) | VISUAL-DRIFT + VARIANT-DRIFT + TOKEN-DRIFT | Rebuild `SmilesBalance` as white V1.1 card: r16 surface-inverse, `heading/lg` 28 points block, tier pill (medallion + 12px label), h12 progress bar (sunken track / positive-subtle fill), `body/md` footer line + gradient `next-tier` text (gold/silver/plat); add `states` axis; drop in-card `cta` — CTA is the shared action-bar below the card (extract with 08's action-bar item); update design.md §Smiles Balance (drop "tier-themed palettes" + in-card CTA) |
| Voucher | final set `Voucher` **28278:13817** (`Display=Light,State=Default` 28278:13818 · `Light,Applied` 28278:13827 · `Dark,Default` 28278:13836 · `Dark,Applied` 28278:13845, all 144×144); page also holds stale iterations: `Voucher/State3` 28278:13787 + `State4` 28278:13778 (144×80), loose `Voucher` frames 28278:13796–13815 (144×112), `Selectors (OLD)` 29158:17350–53, ref screenshot 28278:13816, 🪧 Flow Sign 28278:13777 | 🔵 page | `Display`(Light/Dark) × `State`(Default/Applied), fixed **144×144** ticket tile; Shape = boolean ticket silhouette (r `border-radius/5` 16, side notch cutouts + dashed perforation between content and button zones); Light shape white / Dark shape midnight; Content px `sm` 8 py `lg` 16 centered: title `title/xs` SemiBold 14 lh1.3 (2 lines) + desc `body/md` 14 lh1.4, `color/text/default/default` #191329 (inverse on Dark); Button zone px `md` 12 py `lg` 16, rounded-b 16: tertiary pill button `button/lg` SemiBold **16** `button/tertiary/text/midnight/default` #191329 + 24px icon (p `2xs` 2), label "Apply"; Applied state swaps to green "Applied" (positive) on both displays | `Voucher` — props `value`/`code`/`validity`/`status`(active/redeemed/expired); full-width horizontal coupon row: `surface.raised.default` bg, **1px dashed border** (fake perforation), r16, p `lg`, `heading.xs` value + "Code: …" `body.sm` muted + status `Badge`, opacity .6 when expired; no Display axis, no ticket shape, no Apply button, wrong state model | `voucher.md` — `Display`(Dark/Light) × `State`(Applied/Default) ✓, 144×144 ✓, r16 ✓, Shape · Content · Button ✓ (matches V1.1) | VISUAL-DRIFT + VARIANT-DRIFT + TOKEN-DRIFT | Rebuild `Voucher` as fixed 144×144 ticket tile: notched shape + dashed perforation (SVG/mask, not dashed border), centered `title/xs`+`body/md` content, `button/lg` tertiary Apply w/ 24px icon; replace `status` with `state` Default/Applied + add `display` light/dark; carousel sizing (fixed 144) per design.md slot; confirm redeemed/expired handling with design (no V1.1 equivalent); update design.md §Voucher (states + composition) |

**Verdict tally:** OK 0 · TOKEN-DRIFT 2 · VISUAL-DRIFT 2 · VARIANT-DRIFT 2 · MISSING-IN-CODE 0 · REMOVED-IN-FIGMA 0 · STALE-ANATOMY 0. (Both components carry multiple flags; both anatomy files are accurate.)

---

## Notes per component

### Smiles Balance — page `26610:601` ("   ↳ Smiles Balance 🟣")
- Page is minimal (no changelog/notes/statesheet boards like 08): core symbol `.smiles-balance-card-core` 26610:3492 (353×148) · atoms frame `.next-tier` 26663:89987 · final set `smiles-balance-card` 27131:101113 · `template` section 26663:90113 with two `section` instances, each slotting `smiles-balance-card` (335w) + `action-bar` (335×72) into `.section-body`. The 🟣 page flag is noted verbatim; it is not one of the 🟡/🔵 conventions seen elsewhere — treat as a page-status marker to confirm with design.
- **Final set** axes: `states` = points-activated / points-not-active only. `next-tier` (silver/gold/plat) is a **separate atom set**, swapped inside the footer line — not an axis on the card itself (anatomy file lists both together, which is fair but worth knowing when modeling props).
- **Exact spec** (design-context on 27131:101112): white card `color/surface/base/inverse`, radius `border-radius/5` **16**, overflow clip, 353×148 (hug-width 335 inside section slot).
  - **Header** (pt/px `spacing/lg` 16): text-block = points value + "Points" on two lines, `typography/heading/lg` Suisse Int'l Bold **28px**, lh 1.2, tracking −0.65, `text/action/midnight/default` #191329. Right: **tier pill** — bg `bg/component/light-gray` #f4f3f5, pill radius 100, pl 4 / pr 8 / py 4, gap 4; 32px circular Smiles-loyalty medallion asset + tier label 12px Suisse Book `text/default/dark/low-emphasis` rgba(25,19,41,0.5) ("Silver").
  - **Footer** (px `lg` 16, pt `sm` 8, gap 4, bottom-anchored): `.progress-bar-core` (26663:89931) full-width h **12**, track `color/surface/sunken/inverse` #f7f7fa, bar `color/text/positive/subtle` #54bc72, radius `border-radius/8` 1200; below it the text line `body/md` 14 Regular lh 1.4 `text/default/dark/high-emphasis` #140f21 — "Just XXXX points to your" + **`.next-tier`** gradient-text atom: `title/xs` SemiBold 14 lh 1.3, text fill = radial gradient style **"Gradients (NEW)/Gold"** #e2c668 → #cca951 → #b78d3b (silver 67×18 and plat 88×18 variants use their own gradients).
  - **points-not-active** variant (27131:101114, from set screenshot): "0 / Points", progress bar at 0, footer line "Get XXXX points for activating", **no tier pill and no next-tier gradient text**.
- **CTA moved out of the card**: the template pairs the card with the shared `action-bar` component (h72) inside the same section slot — same shared action-bar as Highlight (08) and the one indexed under 03 Navigation. The card itself has **no button**.
- **Code drift** (`SmilesBalance` in cards.tsx, lines 156–170):
  - Whole visual language is V1.0: gold gradient card `linear-gradient(120deg,#e2c668,#d9b14a)` — those hexes now live on as the V1.1 gold **tier text** gradient, not a card surface. V1.1 card is plain white.
  - Layout horizontal label/points + `Button sm secondary` "Redeem" **inside** the card vs V1.1 vertical header/footer with external action-bar.
  - Type: points `heading.sm` 20 vs `heading/lg` 28; label "Smiles balance" `body.sm` at off-token rgba(25,19,41,0.7) — V1.1 has no such label.
  - Missing: `states` axis, progress bar, tier pill/medallion, next-tier gradient line.
- **Anatomy `smiles-balance.md`**: variants, dims (353×148 + 88/67/60×18 atoms), radius 16 and header · footer parts all match V1.1 — **not stale**.
- **design.md §Smiles Balance** (line 4020): role/slot/fill×hug fine; stale: "States: tier-themed (special palettes)" (V1.1 states are points-activated/not-active; tier expressed via pill + gradient text, not card palettes) and "Composition … + CTA" (CTA is the adjacent action-bar, not part of the card). The dumped `color.special` ramps (mauve/red-sand/teal/burgundy) are not referenced by this component in V1.1.

### Voucher — page `28278:13069` ("   ↳ Voucher 🔵")
- Page contents: 🪧 Flow Sign 28278:13777 (375×812 flow board) · stale iterations `Voucher/State4` 28278:13778 + `Voucher/State3` 28278:13787 (144×80, older squat ticket) · loose mid-iteration `Voucher` frames/instances 28278:13796/13797/13798/13807 (144×112) · reference screenshot rect 28278:13816 · **final set `Voucher` 28278:13817** · four `Selectors (OLD)` instances 29158:17350–53 (labelled OLD, belongs to Selectors, not this component). Only 13817 is published; the rest is scratch left on the page — consistent with the 🔵 (in-progress/review) page flag.
- **Final set** axes: `Display` (Light/Dark) × `State` (Default/Applied), all variants fixed **144×144**.
- **Exact spec** (design-context on 28278:13818 Light/Default):
  - **Shape**: boolean-operation ticket silhouette filling 144×144 — outer radius `border-radius/5` **16**, semicircular **notch cutouts** on left/right edges at the content/button boundary plus a **dashed perforation line** across (visible in set screenshot). Light = white fill; Dark = midnight fill (inverse text).
  - **Content** (px `spacing/sm` 8, py `spacing/lg` 16, centered): title `title/xs` Suisse Int'l SemiBold 14 lh 1.3, up to 2 lines ("Title with 2 lines") + description `body/md` 14 Regular lh 1.4 ("above AED 60+"), both `color/text/default/default` #191329 on Light.
  - **Button zone** (px `spacing/md` 12, py `spacing/lg` 16, rounded-b 16): tertiary **button** instance — pill radius 1200, label `button/lg` SemiBold **16** `color/button/tertiary/text/midnight/default` #191329 + 24px icon frame (p `2xs` 2) with placeholder glyph. Default = "Apply"; **Applied** state renders green positive "Applied" text on both Light and Dark (set screenshot).
  - **Dark display** (design-context on 28278:13836): midnight shape asset; content text `color/text/default/inverse` white; button label `color/button/tertiary/text/inverse/default` white — same geometry/spacing tokens as Light.
- **Code drift** (`Voucher` in cards.tsx, lines 172–187):
  - Entirely different component shape: full-width horizontal coupon row (`surface.raised.default` bg + **1px dashed border** as fake perforation) vs fixed 144×144 vertical ticket tile with real notch cutouts + internal dashed perforation.
  - Props model `value`/`code`/`validity`/`status`(active/redeemed/expired) + status `Badge` + expired opacity 0.6 — V1.1 has `State` Default/Applied and `Display` Light/Dark; no code line, no status badge, no redeemed/expired variants.
  - No Apply button in code; V1.1's only action is the tertiary `button/lg` Apply/Applied.
  - Tokens: `heading.xs` value + `body.sm` muted vs `title/xs` 14 + `body/md` 14 centered; radius 16 is the one thing that matches.
- **Anatomy `voucher.md`**: Display×State matrix, 144×144, r16, Shape · Content · Button — all match V1.1 — **not stale**.
- **design.md §Voucher** (line 4177): "width: fill / fixed (carousel)" — V1.1 is fixed 144 only (carousel tile); "States: active · redeemed · expired (status Badge)" and "Composition: value + code + validity + status Badge" describe the V1.0 code component, not the V1.1 ticket. Same irrelevant `color.special` ramp dump as Smiles Balance.

---

## Cross-cutting actions
1. Both components need ground-up rebuilds against V1.1 — the code exports are V1.0 concepts (gold-gradient balance row, dashed-border coupon row) that share almost nothing with the published V1.1 components beyond radius 16.
2. Shared `action-bar` extraction (08 cross-cutting #2) also unblocks Smiles Balance: the template's CTA is the same action-bar component (26163:72077 family) slotted below the card.
3. Smiles Balance progress bar should consume the audited `.progress-bar-core` primitive (01-primitives), not a bespoke div.
4. Confirm with design: meaning of the 🟣 flag on the Smiles Balance page; whether Voucher redeemed/expired states are intentionally dropped in V1.1 or pending; whether the stale Voucher iterations (State3/State4, 144×112 frames, Selectors OLD) can be cleaned from the page.
5. Update design.md §Smiles Balance and §Voucher (states, composition, fixed-144 voucher sizing) — both currently describe the V1.0 code components; also both sections dump the full `color.special` ramps that neither component uses in V1.1.
