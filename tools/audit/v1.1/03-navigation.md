# V1.1 Audit — 03 Navigation

- **Section:** 03-navigation
- **Audit date:** 2026-07-03
- **Figma file:** `pzm63BTLfPfT1stcF89ILQ` — "e& Consumer App DSL V1.1" (published library)
- **Pages audited:** Top bar 🟡 `22542:13963` · Nav bar `22542:13964` · Action bar `25519:15621` · Tabs `22542:13966` · Section link `25460:83978` · Quick Action `25507:13670` · List Row (page TBD — search in progress)
- **Code:** `packages/react-design-system/src/components/navigation.tsx` (TopBar, NavBar, ActionBar, SectionLink, QuickAction, ListRow) · `packages/react-design-system/src/components/controls.tsx` (Tabs — lives in controls.tsx, not navigation.tsx as design.md implies)
- **Includes:** Tabs (explicitly deferred here by the 02-controls audit)

## Drift table

| Component | Figma node (V1.1) | Flag | Figma spec (variants/dims) | Code (export/props) | Anatomy file | Verdict | Action |
|---|---|---|---|---|---|---|---|
| Top bar | page `22542:13963` 🟡; final set `Master menu component` **29547:11510**; brand header set `Profile header - NEW` **15483:931** | 🟡 | Master: `Large title`(on/off) × `Bottom element`(on/off), 367w, h 112–290; atoms Left/Middle/Right part, Big title, Bottom elements (AI Search/search/Button/Tabs/Chips/OTP/steps/Banner/Carousel); red brand surface, bottom radius 20, glass-md search field. Brand header: greeting 14 Reg + masked number 14 Bold, circle buttons **48×48** white-glass 10%, avatar+tier badge, `quickAction` carousel of 72h dark-glass cards (radius 20, white pill "Get" h36) | `TopBar` — variant default/brand; brand ≈ Profile header (greeting/title/actions/actionBar) but circle buttons **40×40** @18% white, actionBar = single card rgba(0,0,0,.18) radius **14**, title `title.md` 20 vs Figma 14 Bold; no avatar/tier/Link slot; no Master-menu axes (Large title, Bottom element slots) | `top-bar.md` — brand header current; collapsing Small/Middle/Full noted; misses Master-menu set + atoms | VARIANT-DRIFT + VISUAL-DRIFT + TOKEN-DRIFT | Model `Large title` + `Bottom element` slots; fix circle btn 48/10%, actionBar radius 20 + card tokens, number type 14 Bold; add avatar/tier; page is 🟡 — re-verify before build |

| Nav bar | page `22542:13964`; main symbol `navbar` **29317:6983** (370×110); sets `.navbar-tab` 26593:20395, `.navbar-tab-state` 26593:20631, `navbar-tabs` 29372:4769, `.navbar-bg` 29372:4986, `.navbar-profile` 29372:17589, `.navbar-scroll` **31239:84320 (NEW)** | — | Glass pill 338×64, radius 1200; 4 equal tabs 64w; active = white pill (`navbar-tab/surface/focus`) + red icon/label (`text/focus` #e00800); inactive white; label `body/xs` 10 lh1.4; icon box 32 (glyph 24); `page`=home/support/profile/shop/**Plans/Devices/Elife**; `.navbar-profile` logged-in=yes/no; `.navbar-scroll` down 375×46 (collapsed) / up 375×60; **`special` axis published only as "no"** | `NavBar` — frosted pill + midnight scrim gradient + 144×5 home indicator ✓; active white pill + red content ✓; label 10/1.4 ✓; icon 32 box ✓; `avatar` slot ≈ logged-in ✓; `special` prop vestigial (unused in render); no scroll-collapse behavior | `nav-bar.md` — visuals/dims current; **cites dead node `29528-9868`** (not in file; live = 29317:6983) | OK (minor VARIANT-DRIFT + STALE-ANATOMY) | Add scroll-direction collapse (or defer); drop vestigial `special` prop + `focus-special` tokens (removed in Figma); fix anatomy node-id → 29317:6983 |

| Action bar | page `25519:15621`; set `action-bar` **25627:23258** (color-scheme=default 25627:23259 / inverse 26163:72076, 292×72); atoms `.action-bar-core` 25627:23696, `.image-asset` 26563:32026, `.action-bar-trigger` 26714:1810, `.surface-default/-inverse` 26714:1802/1796 | — | Inline action ROW, not a footer: 292×72, radius 20, px16, bg `surface/base/inverse`; 40×40 icon/image square (#f0f0f5, r12) + Title `title/xs` 14 SB + Description `body/md` 14 subtle + ONE trailing trigger (button h40 pill / chevron 24 / switcher); surface-color ×8; NEW set `bill action card` **29807:22045** (State=Compact 350×72 / Expanded 350×148: total+ⓘ+subtext + Pay / Promise-to-pay+Pay-bills buttons) | `ActionBar` — sticky bottom FOOTER: helper text + children button row, bg canvas, 1px top border. Models the V1.0 "sticky action footer" concept; matches none of the V1.1 axes (no color-scheme, icon/image, trigger, surface-color, Bill) | `action-bar.md` — current (axes/dims/rules match V1.1 exactly, incl. "only one trailing action" rule) | VISUAL-DRIFT + VARIANT-DRIFT + MISSING-IN-CODE (bill action card) | Rebuild ActionBar as the inline row (leading visual + text + single trigger); `bill action card` = closest analogue to the current footer — document only (scope-gated); fix design.md Role line ("Sticky action footer" is stale V1.0) |

| Tabs | page `22542:13966`; set `pill-tabs` **25502:12416** (`💡 State=default, Active=no` 25502:12417 / `💡 State=active, Active=yes` 25502:17555, both 101×40); atom `tab-content` 25502:15320 | — | Only 2 published variants (State+Active redundant dual axis); **Global vs Local is NOT a variant axis** — instance-level color overrides documented in statesheet (Global focus = brand red on `tab/surface/global/active` #fce6e6; Local focus = midnight); h `tabs/height/md` 40, **min-w 72, px 8**, radius 1200, label **`title/xs` 14 Semi bold**, optional leading icon 20; loose `tabs` frame 29554:906 = unstructured **48px-tall** explorations | `Tabs` — scope global/local prop ✓ concept; h 40 ✓ pill ✓; but global-active bg = `surface.canvas.brand-muted` **#fff2f2** (should be `tab.surface.global.active` **#fce6e6** — token exists in tokens.ts, unused); local-default text `text.default.default` #191329 vs token #908e9a; label `button.sm` **12 Medium** vs Figma **14 Semi bold**; px 12 vs 8; no min-w 72; no leading icon; `tabs.height.sm` 36 token unused in Figma docs | `tabs.md` — scopes/states/dims current; says "focused = tinted-red (`surface/canvas/brand-muted`)" — wrong token name (actual = `tab/surface/global/active` #fce6e6 ≈ base/brand-muted) | TOKEN-DRIFT + VISUAL-DRIFT (typography) | Re-point Tabs to `color.tab.*` tokens (already in tokens.ts); label → title.xs 14 SB; px8 + min-w 72; add optional leading icon; fix tabs.md token name; watch 48px exploration frame |

| Section link | page `25460:83978`; set `section-link` **25440:14243** (9 symbols, all 40×40); NEW section `Top-bar link` **31511:33083** → second set also named `section-link` **31511:33442** | — | An icon-only **link BUTTON**, not a header row: `size`(lg only) × `surface`(default/Inverse/inverse/glass — messy axis) × `State`(default/focus/disabled); 40×40, radius `border-radius/4` **14**, px 12, inner `icon-size` 24 + chevron-right; sits top-right of a Section header ("in-section" template). Statesheet text says Lg 48/Md 36/Sm 32 but symbols + `section-link/lg` var = **40**. NEW `Top-bar link` (03.07.2026): 40×40 **circle** r1200 glass button (default 15% / focus white / disabled 10%), chevron-left, "atom for the Top-Bar component" | `SectionLink` — a full-width header ROW (title `heading.xs` + "See all" text link + chevron, h 40). Models the composed Section-header pattern, not the published component; no icon-button export, no surface/state variants. `TopBar`'s internal `CircleBtn` (40×40 @18%) approximates Top-bar link (15%) but no focus/disabled and not exported | `section-link.md` — axes/dims/radius current; but "loader" atom doesn't exist (page atoms = icon-size only), sizes bullet repeats the conflicting Lg-48 statesheet text, misses Top-bar link section | VISUAL-DRIFT + VARIANT-DRIFT (+ MISSING-IN-CODE: Top-bar link atom) | Decide naming: keep composed-row `SectionLink` (rename?) and add the icon-button component; get designer ruling on 48-vs-40 statesheet conflict; add Top-bar link atom (or fold into TopBar); fix anatomy loader/size bullets; design.md describes the composed row, not the component |

| Quick Action | page `25507:13670`; main set `quick-action` **27962:37107** (5 symbols); atoms `.quick-action-core` 28365:37221, `.quick-action-variants` 27917:8012, `.quick-action-icons` 27915:4453 | — | Axes `Grid`(On/off) × `Carousel`(on/off — redundant inverse of Grid) × `Row`(Single/Two/Three); **Grid = 2-up** (334w, cells 165×148) / **Carousel = 3-visible bleed** (449w, cells 147×148), gap `spacing/xs` 4; no Carousel+Row=Three symbol. Cell: white `surface/raised/default`, radius `border-radius/6` **20**, p12, **fixed h148**; 40×40 icon tile (#f0f0f5, r12, 32 icon-size box → 24 glyph); Status Badge **lg** (h24 min-w64, `badge/lg` 14) right of header; label `title/sm` **16 Semi bold lh1.3** bottom-aligned in 74h content block; 13 preset `type`s + 13 `Icon type` tiles | `QuickAction` — grid of white cards ✓ r20 ✓ gap 4 ✓ p12 ✓ icon square 40 r12 `surface.base.default` ✓; but `columns` default **3** vs Figma grid 2-up; no carousel mode; minHeight **104** vs fixed 148; label `title.xs` **14** vs `title/sm` **16**; bg `surface.canvas.default` (value #fff ✓, wrong semantic — Figma binds `surface/raised/default`); badge = absolute top-10/right-10 overlay vs in-flow header slot (Status lg) | `quick-action.md` — icon-square/label/badge anatomy + examples current; but "rounded **16**, subtle **border**" wrong (Figma r20 borderless) and "3-up (or 2-up)" stale (grid is 2-up; 3-visible = carousel) | VARIANT-DRIFT + VISUAL-DRIFT + TOKEN-DRIFT (+ minor STALE-ANATOMY) | Add carousel mode + default columns→2; fix cell h148, label title.sm 16, bg→raised token; move badge into header flow; fix anatomy r16/border/3-up bullets |

**Verdict tally:** _in progress_

---

## Notes per component

### Top bar — page `22542:13963` (renamed "   ↳ Top bar 🟡" — WIP flag)
- **Page renamed & flagged:** V1.1 page name is `   ↳ Top bar 🟡` — carries the yellow work-in-progress flag (like Input Field in 02-controls). Content is a mix of V1.1 "final" work and V1.0-era leftovers.
- **V1.1 canonical set:** section `Top bar` **30420:33360** → "Final component" = **`Master menu component` 29547:11510** with axes **`Large title` (on/off) × `Bottom element` (on/off)** — symbols 367w × 290 / 188 / 112 / 214. Screenshot verified: red brand surface (`color/surface/base/brand` #e00800), status bar, back chevron circle (glass), "Link" action right, **Large title** = `heading/lg` 28 Bold white + subtext `body/lg` 16, optional **Bottom element** = glass search field ("Search for feature" + mic, `surface/glass/white/md` #ffffff26, glass blur radius 19); avatar + "Title / 050 123 4567 ⌄" middle part on non-large-title variants.
- **Atoms (all on page, V1.1):** `Left part` 29547:11340 (type = Chevron + Text / Avatar + Text / Chevron / Text / None) · `Middle part` 29547:11391 (Title + Subtitle / Title / Logo / None) · `Right part` 29547:11368 (2 icon buttons / 1 icon button / Link / None / Button) · `Header top part` 29547:11402 · `Bottom part` 29547:11484 · `Big title` 29547:11486 (eyebrow × subtext) · `Bottom elements` 29547:11407 (**Type = AI Search / search / AI Search 2 / Button / Tabs / Chips / OTP / steps / Banner 1 / Banner 2 / Carousel**). Duplicate atom frames also parked loose at 31318:22828/22856/22867.
- **Statesheets:** two `Statesheet` sections (29547:11531, 29547:11563) with **Small / Middle / Full (before scroll)** collapsing states — matches anatomy's "Collapsing variants: Small / Middle / Full".
- **Brand account header:** `Profile header - NEW` **15483:931** — `State`(Default/Scrolling) × `Type`(Multiple accounts/Single number) + bools `quickAction`, `showTierIcon`. Exact spec (design-context on 22565:26231): container 375w, bg `bg/component/red` #e00800, **bottom radius 20**, pb16; greeting "Hi, Ahmed" Suisse Regular **14/18** white; masked number "050 123 4567" Suisse **Bold 14** + chevron; avatar 48×48 white-30% circle with overlapping **Smiles tier badge** 24; right circle buttons (AI sparkle, bell) **48×48**, bg `white-glass rgba(255,255,255,0.1)`, radius 60; `quickAction` row = horizontal carousel of `List item/medium` cards **305×72**, bg **rgba(25,19,41,0.2)** radius **20**, VD asset 40 + Title/Subtitle + white pill button "Get" (h36, radius 24, red label 12). NOTE: this set still binds **legacy token namespaces** (`bg/component/*`, `text/default/white/*`) — pre-V1.1 tokens, consistent with the page's 🟡 status.
- **V1.0-era leftovers still on page:** `Navigation header` 4005:30685 (State=Main Page/Inner page), `Title+Action` 22563:20493 (Type=Default/e&/Account/Subtitle/No title), `Top nav buttons` 15069:1401 (Type=Image/Avata/Avatar Group/Search/Notifications — note Figma typo "Avata"), `*Status-bar` 22563:18548, Banners Carousel 23044:21014.
- **Code drift (`TopBar`):**
  - `variant="brand"` maps to Profile header - NEW conceptually (greeting + masked number + circle actions + actionBar) but: circle buttons **40×40 @ rgba(255,255,255,0.18)** vs Figma **48×48 @ rgba(255,255,255,0.10)**; masked number rendered `title.md` (20 Semi bold) vs Figma **14 Bold**; greeting `body.sm` (12) vs Figma **14**; `actionBar` is a single full-width card with bg `rgba(0,0,0,0.18)` + radius **14** + padding-pill CTA vs Figma's **carousel of 305×72 cards** bg `rgba(25,19,41,0.2)` radius **20** with a proper sm Button (h36); no avatar / tier badge slot; no "Link" right-part option.
  - `variant="default"` (white 56h bar with bottom border) ≈ Master menu `Large title=off, Bottom element=off` in shape, but Figma's default master is **red-surface**, borderless, radius-bottom-20; a plain white top bar exists only in V1.0 leftovers (`Title+Action`). The white default look is closer to V1.0 than V1.1.
  - No support for Master-menu composition: `Large title` (28 Bold + subtext), `Bottom element` slot (search/AI search/Tabs/Chips/OTP/steps/banner/carousel), collapsing Small/Middle/Full.
  - Faux `statusBar` row is a code-only convenience (Figma uses the `*Status-bar` symbol) — acceptable presentational stand-in, not drift per se.
- **Anatomy `top-bar.md`:** brand-header bullets current (greeting/masked number/circle buttons/action bar); "Collapsing variants Small/Middle/Full" current. Missing: Master-menu set + `Bottom element` slot list, and the 🟡 flag. Minor stale only.
- **Tokens observed (variable defs on 29547:11510):** `color/surface/base/brand` #e00800 · `color/surface/glass/white/md` #ffffff26 / `lg` #ffffff33 · `border-radius/8` 1200 · `section-link/lg` 40 · `typography/heading/lg` 28 Bold −0.65 · `typography/body/lg` 16 · `typography/title/sm` 16 Semi bold · glass effect `surface/glass/glass-md` (radius 19).

### Nav bar — page `22542:13964` ("   ↳ Nav bar", no flag)
- **V1.1 structure (section `Navbar` 27838:44704):** main published symbol **`navbar` 29317:6983** (370×110 — glass pill + home indicator over scrim). Building blocks, all live:
  - **`.navbar-tab` 26593:20395** — `page` = home / support / profile / shop / **Plans / Devices / Elife** (56×50 each; 7 destinations, 3 new vs anatomy's list).
  - **`.navbar-tab-state` 26593:20631** — `active` = yes/no × `special` = **no only**. Exact spec (design-context on 29372:4769): tab cell 64w flex-1, py `spacing/xs` 4, radius `border-radius/8` 1200; **active** = inner pill bg `color/navbar-tab/surface/focus` (white), pt2 pb4, label `color/navbar-tab/text/focus` **#e00800**; **inactive** = transparent, label `color/navbar-tab/text/default` (white); label `typography/body/xs` **10px Regular, lh 1.4**; icon-placeholder **32×32** (p4 → 24 glyph).
  - **`navbar-tabs` 29372:4769** — `page-1..4` yes/no booleans (one active at a time), 338×60, radius 1200, 4 equal-flex tabs.
  - **`.navbar-bg` 29372:4986** — 338×64 glass pill background atom.
  - **`.navbar-profile` 29372:17589** — `logged-in` = no / yes (avatar swaps into the profile tab).
  - **`.navbar-scroll` 31239:84320 (NEW axis):** `scroll-direction` = **down** (375×46 — collapsed, icons-only pill) / **up** (375×60 — full icon+label). Scroll-aware collapse behavior new in V1.1.
- **`special` disposition:** only `special=no` variants are published — the **detached red mShop circle (focus-special) no longer exists in Figma**. Code comment agrees ("mShop is now a regular tab — no detached circle") and the render never reads `it.special`, but `NavItem.special?: boolean` is still exported and design.md still lists `navbar-tab.surface.focus-special` / `text.focus-special` tokens (#e00800 / #ffffff) → vestigial prop + orphaned tokens = REMOVED-IN-FIGMA (minor).
- **Code match (`NavBar`):** floating frosted pill (white 15% + blur 19 ≈ Figma glass-md) over midnight scrim gradient ✓ · active = solid white pill with red icon+label via currentColor ✓ · inactive white ✓ · label 10px lh 1.4 ✓ · icon box 32 ✓ · home indicator 144×5 white pill ✓ · equal-width tabs ✓ · `avatar` slot covers `.navbar-profile logged-in=yes` ✓. Gaps: no scroll-direction collapse; pill padding 4 all around vs Figma's per-tab py4 (visually equivalent); glass border `1px rgba(255,255,255,0.22)` is a code-only embellishment (Figma pill has no stroke — cosmetic, sub-flag level).
- **Anatomy `nav-bar.md`:** visuals, tokens, 338×64, destinations, home indicator all current — but the Figma reference **`29528-9868` no longer resolves** (verified: node not found). Live node is `navbar` 29317:6983. Also missing: Plans/Devices/Elife pages and `.navbar-scroll`. STALE-ANATOMY (node-id + additions).

### Action bar — page `25519:15621` ("   ↳ Action bar", no flag)
- **V1.1 set `action-bar` 25627:23258:** `color-scheme` = **default** 25627:23259 / **inverse** 26163:72076 — both **292×72**. Exact spec (design-context on default): container h `scale/72`, px `spacing/lg` 16, radius `border-radius/6` **20**, bg `color/surface/base/inverse` (white); inner `.action-bar-core` h40 = leading `.image-asset` **40×40** (bg `color/surface/base/default` #f0f0f5, radius 12, 32 icon-size inside) + text-block (Title `typography/title/xs` 14 Semi bold lh1.3 `text/default/default`; Description `typography/body/md` 14 Regular lh1.4 `text/default/subtle` #575362, both single-line ellipsis) + trailing `.action-bar-trigger`.
- **Atoms:** `.action-bar-core` 25627:23696 (292×40) · `.image-asset` 26563:32026 (`type` = icon / image, 40×40) · `.action-bar-trigger` 26714:1810 (`trigger` = **chevron** 24 / **button** h40 min-w40 px12 radius-1200 primary red, label `button/md` 14 Medium / **switcher** 40×24) · `.surface-default` 26714:1802 (surface-color = subtle / default / sunken) · `.surface-inverse` 26714:1796 (midnight-base / midnight-raised / midnight-transparent / white-transparent / glass).
- **NEW: `bill action card` set 29807:22045** (section `Bill action card` 28266:64925): `State` = **Compact** (350×72: "Total: AED 435.01 ⓘ / Bill: … · Extras: …" + red Pay pill) / **Expanded** (350×148: total + "Includes basic plan & add-ons" + secondary "Promise to pay" + primary "Pay bills" side-by-side). Atoms `.sub-text` 29807:22311 (`Type` = Bill / Sub-text) and `bill-container` 29807:1761 (220×38). Screenshot verified. **This — not `action-bar` — is the Figma analogue of a sticky bill/CTA footer.** No code export → MISSING-IN-CODE (document only, scope-gated).
- **Code drift (`ActionBar`):** conceptual mismatch. Code renders a **sticky bottom footer** (helper text over a `children` button row, canvas bg, 1px top border) — the V1.0 "sticky action footer" model that design.md's Role/Behavior/States lines still describe ("1-button · 2-button · with helper text"). Figma V1.1 `action-bar` is an **inline tappable row inside Sections/banners** (leading 40×40 visual + title/description + exactly one trailing trigger; rule: "never combine a button and a chevron"). Code has none of: color-scheme, icon/image leading visual, trigger switch (button/chevron/switcher), 8 surface-colors, radius-20 shape, fixed h72.
- **Anatomy `action-bar.md`:** fully current — axes (`color-scheme` × `type` × `trigger` × `surface-color` × `Type` × `State`), dims (350×148 · 292×92 · 350×72 · 292×72 · 292×40 · 220×38), and usage rules all match the live V1.1 page. The drifted artifacts are the code and design.md's Role/Behavior/States lines.

### Tabs — page `22542:13966` ("   ↳ Tabs", no flag) — deferred here by 02-controls
- **V1.1 structure (section `Pill Tabs` 26329:4310):** published set **`pill-tabs` 25502:12416** with only **two variants** — `💡 State=default, Active=no` 25502:12417 and `💡 State=active, Active=yes` 25502:17555 (both 101×40; State and Active are a redundant dual axis encoding one boolean). Atom **`tab-content` 25502:15320** (94×21) = optional `icon-size` 20 + single-line label. Metadata re-verified this session — structure unchanged from the drift-table row.
- **Global vs Local is documentation-level, NOT a variant axis.** The Variants section has separate "Global" (26329:2261) and "Local" (26329:2362) sub-sections, and the Statesheet (26329:2446) defines the scopes in prose: *"Global – top-level tabs that determine the overall content of the page"* / *"Local – tabs used within specific sections or smaller components, updating only the content inside their immediate container"*. Four documented states: **Default (Global)** "neutral colors" · **Focused (Global)** "brand red colors" · **Default (Local)** · **Focused (Local)** "brand midnight color". The local look is achieved via instance-level color overrides on the same `pill-tabs` set (local instances measure 81×40 in the docs vs 101×40 global — same component, shorter label).
- **Tokens (variable defs on 25502:12416, re-pulled this session):** `color/tab/surface/global/default` #f0f0f5 · `color/tab/surface/global/active` **#fce6e6** · `color/tab/text/global/default` #908e9a · `color/tab/text/global/active` #e00800 · `tabs/height/md` 40 · `typography/title/xs` **14 Semi bold** · `icon/lg` 20 · `spacing/sm` 8 (px) · `scale/72` (min-width) · `border-radius/8` 1200. **No `local`-scope variables are bound in the published set** — consistent with local being instance overrides, not variants.
- **On-page anatomy annotations (26329:3863):** width hug (110px in the example, varies with label) · height fixed **40** · radius 1200 · icon **20** · label single line, never wraps.
- **Watch: loose `tabs` frame 29554:906** — six unpublished `pill-tabs` explorations at **117×48** (h 48, px 16, icon-20 + label) parked next to the section. If published, height/padding change; not actionable yet.
- **Code drift (`Tabs` — lives in `controls.tsx`, not `navigation.tsx` as design.md's grouping implies):** `scope: 'global' | 'local'` prop matches the Figma scope concept ✓, h 40 ✓, pill radius ✓, horizontal scroll ✓. But every color is bound to the **wrong token** even where the rendered value is close: global-active bg = `surface.canvas.brand-muted` **#fff2f2** vs spec `tab.surface.global.active` **#fce6e6** (visible value drift; the correct token already exists in tokens.ts `color.tab.*` — unused); global-default text = `text.default.muted` #908e9a (value-matches `tab.text.global.default` — semantic drift only); local-active bg = `surface.base.midnight` (value-matches `tab.surface.local.active` #191329 — semantic drift only); **local-default text = `text.default.default` #191329 vs spec `tab.text.local.default` #908e9a (value drift)**; local-default border = `border.solid.default` vs spec `tab.border.local.default` rgba(25,19,41,0.5). Typography: `button.sm` **12 Medium** vs Figma `title/xs` **14 Semi bold**. Padding: `space('md')` **12** vs Figma `spacing/sm` **8**. Missing: min-width 72 (`scale/72`), optional leading icon (tab-content atom), and any badge slot. `tabs.height.sm` 36 exists in tokens.ts but is not bound anywhere on the Figma page (only `tabs/height/md`).
- **Anatomy `tabs.md`:** scopes/states/dims/tokens line all current except one wrong token name: "focused = tinted-red pill (**`surface/canvas/brand-muted`**)" — the actual Figma binding is **`color/tab/surface/global/active`** #fce6e6 (≈ but not = brand-muted #fff2f2). Also cites sizing as `size/tabs`; the live variable is `tabs/height/md`. Node refs `22542-13966` / `25502-12416` valid.
- **design.md `#### Tabs`:** Role/Behavior/States current (`color/tab` states, tabs height token ✓). Composition says "tab items (label, optional **badge**)" — the published `tab-content` atom is icon + label only; no badge part exists in V1.1 (minor stale).

### Section link — page `25460:83978` ("   ↳ Section link", no flag)
- **Identity finding: the Figma component is an icon-only chevron LINK BUTTON, not a header row.** Page definition: *"A specific link button that leads to respective page, based on which content particular section is using."* The "Template — in-section" frame shows it parked at the **top-right of a Section header** (white and red Section examples). The title + "See all" composition that code renders is a Section-header pattern, not this component.
- **Main set `section-link` 25440:14243** (section `Section link` 26932:8601, changelog status **Done**): 9 published symbols, all **40×40** — `📐 size` (lg only) × `🎨 surface` × `💡 State` (default/focus/disabled). The surface axis is messy: **four values with a casing collision** — `default` (all 3 states), `Inverse` capital-I (all 3 states), `inverse` lowercase (default only), `glass` (focus + disabled only). Looks like a mid-flight surface rename left both spellings live.
- **Exact spec (design-context on the set):** container 40×40 (`section-link/lg` var), px `spacing/md` 12, radius `border-radius/4` **14** (rounded square, not a circle); inner `icon-size` **24** box (p 2, r 1200) with chevron-right glyph. Surface fills: default → `surface/raised/default` (white) · Inverse → `surface/base/default` #f0f0f5 · inverse (lc) & glass/disabled → `surface/glass/white/lg` rgba(255,255,255,0.2) · glass/focus → `surface/base/inverse` (white).
- **Internal Figma size conflict (same pattern as Switcher in 02-controls):** the Statesheet says *"The three sizes are Lg: 48x48px, Md: 36x36px, Sm: 32x32px. Padding: 8px for small & medium and 12px for large"*, but every published symbol is 40×40, the bound variable is `section-link/lg` = **40**, and the doc examples measure 40/36/32. Code tokens.ts `sectionLink` (sm 32 / md 36 / lg 40) matches the variables, not the statesheet prose. Only **lg is published as a variant** — md/sm exist as doc-example instances and size variables only. Needs a designer ruling on 48-vs-40 (recommend 40: two of three sources agree).
- **NEW section `Top-bar link` 31511:33083** (changelog **03.07.2026 "V1 created"**, Done — brand new): hosts a second set **also named `section-link`** (31511:33442, naming collision) with only `💡 State` = default/focus/disabled. Spec: 40×40 **full circle** (r `border-radius/8` 1200), px 12 (statesheet prose says padding 8 — another minor internal conflict), glass effect `glass-md`; default = `surface/glass/white/md` rgba(255,255,255,0.15) · focus = `surface/base/inverse` white · disabled = `surface/glass/white/sm` rgba(255,255,255,0.1); chevron-left glyph. Doc: *"Used specifically as an atom for the Top-Bar component"*; states apply on darker surfaces (brand/midnight/image); *"besides chevron, there can be used any other contextual icon."* This is the glass back/icon circle the Top bar audit saw on the red header.
- **Code drift (`SectionLink`):** renders the composed header ROW — title `heading.xs` + trailing "See all" `button.sm` red text + `›` glyph, h 40 (h matches `sectionLink.lg` 40 ✓, though hardcoded not token-bound). It is a useful pattern but it is **not the published component**: no icon-button rendering, no surface (default/Inverse/glass) or state (focus/disabled) variants, no radius-14 square, no 24 icon-size box. The actual Figma `section-link` button has **no code counterpart**. Related: `TopBar`'s internal `CircleBtn` (40×40 @ rgba(255,255,255,0.18)) is an unexported approximation of the new Top-bar link atom (40×40 @ 0.15, focus/disabled missing).
- **Anatomy `section-link.md`:** variants line current (matches the messy 4-value surface axis verbatim ✓); 40×40 ✓; radius 14 ✓. Stale bits: "Anatomy parts: **loader** · icon-size" — the page's Atoms block lists **icon-size only**, no loader exists; the Specs bullets reproduce the conflicting statesheet sizes (Lg 48) against its own "Dimensions: 40×40" line; the new Top-bar link section is absent.
- **design.md `#### Section link`:** Role "Section header + see-all", composition "title + 'see all' link/chevron" — this describes the **composed row / current code**, not the published component. Height "fixed (section-link token)" ✓. States "with/without action · with/without chevron" are not Figma axes. Needs re-pointing at the button component (or an explicit note that code models the composed pattern).

### Quick Action — page `25507:13670` ("   ↳ Quick Action", no flag)
- **Naming split in Figma:** the page/tab is "Quick Action" but the V1.1 doc section is **`Quick-task` 28365:35049** and the cell frame inside instances is named **`quick-task-core`** — while the published set + atoms use the `quick-action` prefix. Mid-flight rename ("Quick Task" → "Quick Action"?); an **empty legacy section `Quick Task` 27917:7997** is still parked on the page.
- **Main set `quick-action` 27962:37107** (inside section Quick-task) — **5 published symbols**, axes `Grid`(On/off) × `Carousel`(on/off) × `Row`(Single/Two/Three): Grid=On Single 27962:37103 **334×148** / Two 27962:37102 334×300 / Three 27962:37104 334×452; Grid=off+Carousel=on Single 27962:37105 **449×148** / Two 27962:37101 449×300. Grid and Carousel are a **redundant inverse pair** (same pattern as Tabs' State/Active dual axis — one boolean encoded twice). **No Carousel + Row=Three symbol** is published.
- **Layout math (screenshot + metadata verified):** Grid = **2 columns** of 165×148 cells (2×165+4 = 334); Carousel = **3 visible** 147×148 cells with edge bleed (3×147+2×4 = 449); cell gap `spacing/xs` **4** both ways; row gap 4 (Two = 148×2+4 = 300; Three = 148×3+8 = 452).
- **Cell spec (design-context on 27962:37103):** `.quick-task-core` = bg `color/surface/raised/default` #ffffff, radius `border-radius/6` **20**, padding `spacing/md` 12, internal gap 10, **fixed height 148**, no border. Header row (gap `spacing/sm` 8): `.quick-action-icons` tile **40×40** — bg `color/surface/base/default` #f0f0f5, radius `border-radius/3` **12**, p4 wrapping a **32px `icon-size`** box (p4, r1200 → 24 glyph) — same construction as Action bar's `.image-asset`; badge slot right-aligned **in-flow** (flex-col items-end) = **Status Badges lg**: h **24**, min-w **64**, px8 py4, radius `border-radius/2` 8, bg `color/green/500` #6ced90, text `badge/lg` 14 Book 450 lh100 `color/badge/text/status/positive` #191329. Content block h **74**, label bottom-aligned: `typography/title/sm` **16 Semi bold lh 1.3** `text/default/default` #191329, ellipsis.
- **Atoms:** `.quick-action-core` **28365:37221** (`variant` = Carousel 147×148 / Stack 165×148) · `.quick-action-variants` **27917:8012** (`type` ×13, all 147×148) · `.quick-action-icons` **27915:4453** (`Icon type` ×13, all 40×40). The 13 preset types: **quick-pay, order-track, sim-activate, sim-replace, addon-buy, devices, device-buy, plan-change, live-chat, contact-us, subscriptions, mParking, all-services**. Labels seen: Quick Pay & Recharge · Track Your Order · mParking · Subscriptions · Replace SIM · My Devices · All Services (matches anatomy examples). Most of these glyphs (mParking, all-services, sim-replace…) have **no counterpart in `@eand/icons`** — same mid-migration gap flagged in 01-primitives.
- **Templates ("Final components" 27094:3769):** quick-task grids composed inside `section` instances — always **2-column** stacks; the 3-across usage appears only as the bleeding carousel. Doc section has Grid + Carousel measure sheets (cell 165 vs 147) and numbered anatomy bubbles.
- **Code drift (`QuickAction`):** closest code match in this section after NavBar — white card grid ✓, radius 20 ✓, gap 4 ✓, padding `md` 12 ✓, icon square 40×40 r12 `surface.base.default` ✓, left-aligned bold label bottom ✓, badge slot ✓. Gaps: `columns` **default 3** vs Figma grid = **2-up** (3-up exists only as carousel); **no carousel mode** (horizontal scroll + bleed) and no Row constraint (acceptable — rows emerge from item count); cell `minHeight: 104` vs Figma **fixed 148**; label `title.xs` (**14** SB) vs Figma `title/sm` (**16** SB lh1.3); card bg `surface.canvas.default` — renders #ffffff so **value matches**, but semantic binding should be `surface.raised.default` (same semantic-drift pattern as Tabs); badge is an **absolute top-10/right-10 overlay** vs Figma's in-flow header slot sized as Status Badge **lg** (code Badge also lacks the lg h24/min-w64 spec per 01-primitives).
- **Anatomy `quick-action.md`:** cell anatomy (grey icon square top-left + left-aligned bold label + Active/New badge) and examples current; node ref `25507-13670` valid. Stale: "**rounded 16, subtle border**" — Figma is radius **20** and **borderless** (the borderless fact is even called out in design.md); "**3-up (or 2-up)** grid" — published grid is 2-up only; misses the Grid/Carousel/Row axes, 148 fixed height, and the 13 preset types.
- **design.md `#### Quick Action`:** Role/Behavior/Composition current — "borderless white cards (radius 20, 4px cell gap), grey radius-12 icon square, optional count badge" all verified ✓. Misses only the Carousel axis and the Figma-side "quick-task" naming.
