# V1.1 Audit — 07 Cards

- **Section:** 07-cards
- **Audit date:** 2026-07-03 (Service + Recommendation completed 2026-07-04)
- **Figma file:** `pzm63BTLfPfT1stcF89ILQ` — "e& Consumer App DSL V1.1" (published library)
- **Pages audited:** General `26760:100129` · Product `25701:12472` · Deals for you 🟡 `25717:33323` · Plans `25915:74211` · New on e& `25915:75766` · Service 🟡 `26019:79144` · Recommendation (none found — see notes)
- **Code:** `packages/react-design-system/src/components/cards.tsx` (PlanCard, ProductCard, DealCard, NewCard, ServiceCard) · `packages/react-design-system/src/components/layout.tsx` (Card)
- **Skipped:** Highlight, SmilesBalance, Voucher (sections 08/09 — other agents)

## Drift table

| Component | Figma node (V1.1) | Flag | Figma spec (variants/dims) | Code (export/props) | Anatomy file | Verdict | Action |
|---|---|---|---|---|---|---|---|
| General card | set `General-card` **28463:12391**; atoms `.card-general` 26825:101736, `.card-general-core` 28379:3531, `.default-surface` 26770:100166, `.inverse-surface` 26770:100161 | — | Set = row arranger: `Rows`(1\|2) × `Grid<->Carousel`(no\|yes), 336×224 / 336×452, 4px gap. Cell `.card-general`: `surface`(default\|inverse) × `Carousel<->Grid`, grid 166×224 / carousel 147×224, h `rem/14` 224, radius **`border-radius/6` = 20**; core: `AlignmentTop<->Bottom`(On\|off) × `Size`(Sm\|Md\|Lg media slot); centered `title/sm` SemiBold 16 + centered 64px media; surface-color atoms ×7 (subtle/default/sunken + midnight-base/raised, glass-midnight/white) | `Card` (layout.tsx) — media/title/body/action slots, left-aligned, bg `surface.raised.default`, 1px border, radius **16** (`borderRadius.5`), no surface/alignment/size/rows axes | `general.md` — axes + dims all match V1.1 (current) | VISUAL-DRIFT + VARIANT-DRIFT + TOKEN-DRIFT | Rebuild Card anatomy (centered title-over-media tile, align top/bottom, media Sm/Md/Lg); add surface=inverse + 7 surface-colors; radius 16→20; add Rows/Carousel arrangement (or delegate to Section) |
| Product (card-features) | components `card-features-addon` **25893:54098** · `-product` **26522:14492** · `-category` **26521:1397** (frame `card feature` 28906:1765, section `Product-card` 27032:46756); atom `.features-price` 26549:1110 | — | 3 components, each **229×300** (h `section/body/height/row-4`), radius **`border-radius/6` = 20**, bg `surface/raised/default`, no border; media panel = `.card-bg-color` atom tint, inner radius `border-radius/7` 24; title `heading/sm` Bold 20 (category: `heading/md` 24); price atom `type=smiles\|aed` × `priceMonthly`, `body/xl` Regular 20 + 20px Smiles smiley asset; Offers badge **lg** (h24, min-w 64) top-right 16px inset; bools: badge, priceLabel, topLine, image, category, price | `ProductCard` — one component: eyebrow/image/discount/price/pts/tint, w **200**, minHeight 280/200 (hug), radius **16**, 1px border, title `title.sm` 16, price `title.sm` + 😊 emoji, badge **sm**, grey `imageBox` h140, `tint` = raw color | `product.md` — 3 types + `.features-price` + AED/PTS current; "rounded 16" stale (Figma 20); dims absent | VARIANT-DRIFT + TOKEN-DRIFT + VISUAL-DRIFT | Split into 3 explicit types (or `type` prop); fix dims 229×300, radius 20/24, drop border; price atom (smiles/aed, body.xl, real Smiles asset); badge → lg @16px inset; bind tint to `atom-surfaces.*`; fix anatomy radius |

| Deals for you | component `deals-card` **25973:22611** (section `Deals for you` 30420:34446); atoms `.deals-card-core` 25721:35140, `.default-surface/default` 25973:18329, `.footer/Default` 25928:216907 | 🟡 | Single component **166×224**, radius `border-radius/5` 16, bg white, p12; anatomy = Top-line `body/sm` subtle + 2-line title `title/sm` 16 + **logo-row** (24px logos + "+4" chip) + footer h80: Offers badge **md** + struck-through old price `body/sm` + "AED 400" `title/md` SemiBold 20 + **feedback pill** (👍 120.8k / 👎, h24 glass). **No image.** Banner rules: 343×172, light grey, e& apps logos animated loop, 72×72 highlighted, max 7 carousel items | `DealCard` — image-led card: 120px imageBox + title + subtitle + badge top-left, w **240**, 1px border, radius 16 ✓ | `deals-for-you.md` — dims 166×224/128×80/84×84 + banner specs + radius 16 current; atom list omits `.footer/Default` + logo-row | VISUAL-DRIFT + VARIANT-DRIFT (page 🟡) | Rebuild DealCard to V1.1 anatomy (top-line/title/logo-row/discount-price/feedback); drop image/subtitle slots or confirm w/ design; deals **banner** (343×172) has no code export — document only; re-audit when 🟡 drops |

| Plans (plans-mini) | set `plans-mini` **26003:40647** (default 26003:40646 / inverse 26003:40648); atoms `plans-mini-core` 26003:40543, `.surface-default` 25973:27598, `.surface-inverse` 25973:27593 | ⚠ | `color-scheme`=default\|inverse, **222×300**, radius 16, p16, **no border**; surface-colors: default = subtle/default/sunken, inverse = midnight-base/raised/glass-midnight/glass-white/**brand**; "Postpaid" `body/md` 14 + Offers badge **lg** h24 top-right; title `heading/md` Bold 24; logo-row **32px** logos + "+N" (statesheet says **40×40** — Figma internally inconsistent); "from" `body/md` + "AED 1250/mo" `title/md` SemiBold 20; bools services/badge | `PlanCard` — variant default\|brand\|midnight, w **210**, minHeight **224**, radius 16 ✓, p lg ✓, 1px border on default, category `body.sm`, badge **sm** (hardcoded white span on dark), title `heading.sm`, SmilesRow 24px, price `title.sm` 16 | `plans.md` — describes the code-era card (~210, subtle border, default/brand/midnight variants) not V1.1 axes/dims | VARIANT-DRIFT + TOKEN-DRIFT + STALE-ANATOMY | Re-model variant → color-scheme × surface-color; fix 222×300, drop border, badge→lg, title→heading.md, price→title.md 20, logos→32 (confirm 32 vs 40 with design); refresh plans.md |

| New on e& | set `new-on` **26523:22852** (selected=yes 26523:22851 / no 26523:22853); atom `.new-on-core` 26523:22827 | — | `selected`=yes\|no, **170×300**; story tile: image frame r **`border-radius/7` 24** (inner r20, p4), **no card surface/border**; selected = 2px gradient outline (`border/lg`; red→green ring, code-fallback `light-green/base` #47cb6c); "New card" badge `badge.surface.offers.new-card` #0e2916, **md** h20, top-left; caption `body/md` 14 centered, 2 lines / max 34 chars; rules: outline+badge+gradient only on **last** carousel card, for 3 days | `NewCard` — boxed card: `surface.raised.default` + 1px border + r16 + `imageBox` h140 r12 + `title.sm` left in `md` padding, w 200; no selected, no new-card badge | `new-on-eand.md` — variants (selected no/yes), dims 170×300, `.new-on-core` all current | VISUAL-DRIFT + VARIANT-DRIFT + TOKEN-DRIFT | Rebuild as story tile (no surface, image r24, centered `body.md` caption); add `selected` outline + New-card badge; fix 170×300; encode last-card/3-day rules as docs |
| Service | atom `.service-card-core` **26592:14455**; sizes set `.service-card-sizes` 27216:41370 (`size=grid` 27216:41369 / `size=carousel` 27216:41368); row arranger `service-card` **28165:35653** (Grid\|Carousel × Rows ×5); section set `service-card-section` **27474:94804** (Layout ×5); section `Service Card` 27474:45670 | 🟡 | Cell **109×148** grid / **128×148** carousel (h `section/body/height/row-2` 148), radius **`border-radius/6` = 20**, white fill, **no border**, p top8/side8/bottom16; centered **48px circular** `icon-size` slot (category=3d-icon, `4xl`, r `border-radius/8` 1200) + centered 2-line label `title/xs` SemiBold **14** lh1.3 (h40); Offers badge **md** "New" h20 min-w56, surface `badge/surface/offers/new-plan` #0e2916, **top-left 8px** inset; rows 335w grid / 392w carousel, 4px gap, Rows=1\|2\|3 (no 3-row carousel); section layouts 367w ×5 | `ServiceCard` (cards.tsx) — `<button>`: 34px text-span icon (default '◍' glyph) + `body.sm` 12 label + free `badge` ReactNode top-left 8px ✓; minHeight 116 hug-w, radius **16**, **1px `border.solid.subtle`**, bg `surface.canvas.default`, p `md` 12 uniform; no size/rows/section axes | `service.md` — cell anatomy (3D icon centered + centered bold label + New badge dark-green top-left) current; **"rounded 16" stale** (Figma 20); no dims, no size axis, no arranger/section sets | TOKEN-DRIFT + VISUAL-DRIFT + VARIANT-DRIFT (+ minor STALE-ANATOMY; page 🟡) | Radius 16→20, drop border, fix h148 + `size` grid109\|carousel128, p 8/8/16; real 3d-icon slot (48 circular) — no glyph placeholder; label → `title.xs` 14 centered 2-line; badge → Offers md live-data statuses (×12); add rows/section arrangement (or delegate to Section); fix `service.md` radius; re-audit when 🟡 drops |
| Recommendation | — **none found** (TBD since V1.0; no node-id in AUDIT.md; page list + published-library search both come up empty) | TBD | No V1.1 page, section, or published component named "Recommendation"; only reference anywhere is design.md's screen-mapping row listing it in the card-carousel family; nearest library match by semantic search = `Action card` (different component, Quick-tasks family) | — no export (correct — nothing to build against) | — none | — not auditable (ABSENT both sides — no drift verdict; still TBD) | No action; **never build speculatively**; watch for a future "Recommendation" page drop (or ask design whether it was folded into Deals-for-you/Action card); keep AUDIT.md row as TBD |

**Verdict tally (final — all 7 entries):** VISUAL-DRIFT 5 (General, Product, Deals, New on e&, Service) · VARIANT-DRIFT 6 (all six audited cards) · TOKEN-DRIFT 5 (General, Product, Plans, New on e&, Service) · STALE-ANATOMY 2 (Plans `plans.md`; Service `service.md` minor) · OK 0 · MISSING-IN-CODE 0 · REMOVED-IN-FIGMA 0. Recommendation = TBD/absent both sides — not counted. Pages flagged WIP: Deals for you 🟡, Service 🟡 (re-audit both when flags drop). (Components carry multiple flags.)

---

## Notes per component

### General card — page `26760:100129`
- Page name "   ↳ General", no 🟡/🔵 flag; section is literally named **"Component name" 28622:2335** (template placeholder never filled in — flag to design).
- Top-level set `General-card` **28463:12391** (376×980) is an **arranger**: `Rows=1|2 × Grid<->Carousel=no|yes` — symbols 28463:12356 (336×224), 28463:12392 (336×452), 28463:12429 (336×224). Only 3 of 4 combinations published (no Rows=2 + Carousel).
- Cell atom `.card-general` **26825:101736**: `surface=default|inverse × Carousel<->Grid=No|yes`; grid cell 166×224, carousel cell **147×224**. Inner `.card-general-core` **28379:3531**: `AlignmentTop<->Bottom=On|off × 📏Size=Sm|Md|Lg` (165×224). Surface atoms: `.default-surface` 26770:100166 (surface-color = subtle/default/sunken) and `.inverse-surface` 26770:100161 (midnight-base/midnight-raised/glass-midnight/glass-white), 84×84 swatches.
- Exact render (design-context on 28463:12356 → cell 26802:101628): h 224 (`rem/14`), radius **`border-radius/6` = 20px**, 4px gap between cells; content = centered 2-line title `typography/title/sm` Suisse Int'l SemiBold 16 (pt `spacing/3xl` 32, px `spacing/md` 12) over a centered 64px (`scale/64`) media/icon slot; sampled surface fill `color/surface/raised/midnight` #312c40.
- Statesheet notes (28622:2454/2459, 28622:2512, 28778:779/784): default/inverse driven by section context; axes = title alignment (top/bottom) × layout (grid/carousel); Sm/Md/Lg control the **media slot** size; "All measurements are tokenized".
- **Code drift:** `Card` in `layout.tsx` is a generic left-aligned media/title/body/action card on `surface.raised.default` with 1px border and radius fallback 16 — different anatomy (no centered title-over-media), no inverse/surface-color, no alignment/size axes, radius token off by one step (`5`→16 vs Figma `6`→20). `design.md` §General ("media/icon + title + body + optional Button/Badge") describes the code, not V1.1 Figma — stale.
- Anatomy `general.md` matches V1.1 (all axes incl. the 7 surface-colors, dims 336×452/336×224/166×224/165×224/147×224/84×84) — current.

### Product (card-features) — page `25701:12472`
- Page "   ↳ Product", no flag; section **`Product-card` 27032:46756**. Not a variant set: **three sibling components** in frame `card feature` 28906:1765 — `card-features-addon` **25893:54098**, `card-features-product` **26522:14492**, `card-features-category` **26521:1397**, each **229×300**.
- Shared shell: h bound to **`section/body/height/row-4` = 300**, radius **`border-radius/6` = 20px**, bg `color/surface/raised/default` #fff, **no border** (the 4px white ring on the media panel is `border/interactive/inverse/focus` — a focus artifact, not a resting border).
- **addon** (statesheet 28927:20537: "For service add-ons — data plans, voice packs, roaming bundles. No image; content slot expands to fill. Uses Smiles points pricing by default."): header block 176px on `.card-bg-color` default tint (`atom-surfaces/default` #e4e3ea, inner radius `border-radius/7` 24, p `spacing/lg` 16) with top-line `body/md` subtle + Offers badge; addon name `heading/sm` Bold 20 lh 1.35; footer "from" + `.features-price` type=smiles.
- **product** (27032:46880: image top, product name below): media panel h **160** on `.card-bg-color` tint with badge overlaid (design shows top 12 vs statesheet rule 16 — minor internal inconsistency); footer title `heading/sm` Bold 20 — note its text binds `text/default/dark/high-emphasis` #140f21, not `text/default/default` #191329 (inconsistent token within Figma) — then "from" + `.features-price` type=aed.
- **category** (27032:46875: "The card-bg-color carries category meaning (orange = phones, etc.). Image sits below the category label"): full-card `.card-bg-color` orange `atom-surfaces/orange` #fcf3eb; "Top-line" `body/md` subtle + "Category" `heading/md` Bold 24; centered 112px image; "from" + AED price. Bools: badge/category/image/label/price/topLine.
- Common atom `.features-price` **26549:1110** (`type=smiles` 26549:1109 / `type=aed` 26549:1111, 192×28; bool `priceMonthly`): `body/xl` Regular 20; smiles = 20×20 Smiles smiley bitmap + "200" + "PTS" + "/mo"; aed = "AED" + value + "/mo".
- Statesheet rule (27032:46933, repeated 29102:2179): **"Badge placement will always be aligned top-right with 16px padding from top and right"**; badge used is Offers **lg** (h `scale/24`, min-w 64, `badge/lg` 14).
- Common-states sheets: Badge on/off (addon) · Smiles↔AED (product) · Price-label on/off (category).
- **Code drift:** `ProductCard` is one merged component — no addon/product/category distinction; w 200 vs 229, min-height vs fixed 300; radius 16 vs 20 (media inner 24); adds a 1px border Figma doesn't have; title `title.sm` 16 vs `heading/sm` Bold 20 / `heading.md` 24; price `title.sm` vs `body/xl` Regular 20; `pts` renders a literal 😊 emoji vs the Smiles asset; badge `size="sm"` vs Figma lg with 16px inset; grey `imageBox` (surface.base.default, h140, r12) vs 160px `.card-bg-color` tinted panel r24; `tint` accepts any string instead of `tokens.color.atomSurfaces` values.
- `product.md` anatomy — the 3-type breakdown, `.features-price` AED/😊 PTS, and per-type anatomy are current; "rounded 16" line is stale (Figma = 20) and no dims are recorded. `design.md` §Product ("image + title + price + Badge + CTA", "out-of-stock/disabled" state) lists a CTA and a disabled state that don't exist in V1.1 — stale.

### Deals for you 🟡 — page `25717:33323`
- Page name "   ↳ Deals for you 🟡" — **WIP flag**; section `Deals for you` 30420:34446 labels `deals-card` **25973:22611** (166×224) as "Final component" (component is slash-named `deals-card/Default`; single state, no variant set).
- Exact render (design-context on 25973:22611): radius `border-radius/5` **16**, surface atom `.default-surface/default` = `color/surface/base/inverse` #fff, padding `spacing/md` 12; **no border, no image**. Anatomy top→bottom:
  1. "Top-line" `body/sm` 12 `text/default/subtle`
  2. Title "Item Name 2-lines max" `title/sm` SemiBold 16
  3. **logo-row** instance (24px circular brand logos + `more-items` "+4" chip, `surface/glass/midnight/lg` rgba(25,19,41,.3), `body/xs` 10 white), py 12 — reuses the Logo-row primitive audited in 01-primitives
  4. `.footer/Default` **25928:216907** (128×80, h `scale/80`): optional discount row = Offers badge **md** (h `scale/20`, min-w 56, `badge/md` 12) + line-through "AED 500" `body/sm` `text/default/muted`; price "AED 400" `title/md` SemiBold 20 `text/default/dark/high-emphasis` #140f21; **feedback-row** pill 105×24, bg `surface/glass/midnight/sm` rgba(25,19,41,.07), r16 — thumbs-up + "120.8k" `body/sm` subtle + divider + thumbs-down (16px icons).
- Statesheet notes: banner = header + e& apps logos, **343×172**, background "always Light Grey", **max 7 items in carousel**; logos animated, auto-scroll left, looped; one logo always highlighted at **72×72**; "Find animation … HERE" link. (Hidden stale bullets remain in the file: "expanded state is a bottom sheet", "banner color is always Red".) The 343×172 **deals banner** exists only as page documentation/examples — no published component set and no code export (documented-only; watch when 🟡 drops).
- In-section template: 5-card carousel rows at 170px pitch (4px gap) + `add-trigger` (335×72) below the row.
- **Code drift:** `DealCard` is an image-led tile (120px `imageBox` + title + subtitle + badge overlay top-left, w 240, 1px `border.solid.subtle`) — V1.1 deals-card has no image, no subtitle, no top-left badge; missing logo-row, struck-through discount price, `title.md` 20 price line, and the like/dislike feedback pill; width 240 vs 166. Radius 16 is the only matching spec.
- `deals-for-you.md` anatomy — dims (166×224 · 128×80 · 84×84), radius 16, banner specs (343×172, 72×72 highlighted logo) all match V1.1; atom list (".default-surface/default · .deals-card-core") omits `.footer/Default` and the logo-row integration — minor gap, not stale. `design.md` §Deals for you ("image + offer text + Badge + CTA", "expiring/limited" state) describes the pre-V1.1 image card — stale.

### Plans (plans-mini) — page `25915:74211`
- Page "   ↳ Plans", no flag — but the section is misnamed **"Buttons" 25973:27475** and the atoms sub-heading above `plans-mini-core` reads **"deals-card-core"** (25973:27634) — two copy-paste errors to flag to design.
- Set `plans-mini` **26003:40647**: `color-scheme=default` 26003:40646 / `color-scheme=inverse` 26003:40648 — both **222×300** (h `section/body/height/row-4`), radius `border-radius/5` **16**, padding `spacing/lg` 16, **no border**; bg atom `.surface-default` = `surface/base/inverse` #fff. Core `plans-mini-core` **26003:40543**.
- Surface-color atoms: `.surface-default` 25973:27598 (subtle / default / sunken) · `.surface-inverse` 25973:27593 (midnight-base / midnight-raised / glass-midnight / glass-white / **brand** — brand red lives here, one more option than General's inverse set).
- Exact render (design-context on 26003:40646): header 112px = "Postpaid" `body/md` 14 `text/default/subtle` + **Offers badge lg** (h24, min-w 64, `badge/lg` 14) top-right; title "Entertainment plans" `heading/md` Bold **24** lh 1.35 (2-line max). Footer 112px = logo-row (**32px** circular logos, gap 8, `more-items` "+4" chip glass-midnight-lg / `body/xs` 10) + "from" `body/md` 14 subtle + price "AED 1250/mo" `title/md` SemiBold **20**.
- Statesheet (29820:78889 ff): 3 states — services-on+badge-on / services-on+badge-off / services-off+badge-off; services row optional (bundled add-ons); badge optional (Discount/New); **max 3 logos + "+N"**; title and rate mandatory. Anatomy sheet (29820:79037 ff): 5 elements (Plan label / Badge / Title / Logos row / Cost); padding 16 all sides; badge top-right hugs; logos 8px gap from title; **"Logo size: 40x40px"** — contradicts the published symbol's 32px logos → internal Figma inconsistency, needs designer ruling.
- Card background sheet: Dark/Light/Grey/Red examples; "Colors should be flexible… no assigned color for any plan"; "Background color is content-driven, not status-driven"; "Badge color contrasts with card background (red badge on light cards, white/light badge on dark cards)".
- **Code drift:** `PlanCard` matches the known recent facts (Postpaid eyebrow, Discount badge, SmilesRow, from-price, variants default|brand|midnight) but drifts on: variant model (3 hardcoded variants vs color-scheme × 8 surface-colors), dims 210×(min)224 vs fixed 222×300, 1px border on default (Figma none), badge `size="sm"` vs lg (the white-on-dark badge is a hardcoded `<span>` rather than a badge variant — the contrast *rule* itself matches Figma), category `body.sm` vs `body/md`, title `heading.sm` vs `heading/md` 24, price `title.sm` 16 vs `title/md` 20, SmilesRow hardcoded 24px vs 32 (or 40 per statesheet).
- `plans.md` anatomy is code-era: "~210, rounded 16, subtle border, variants default (white) · brand · midnight" — V1.1 axes (color-scheme + surface-color atoms) and 222×300 dims missing → STALE-ANATOMY. `design.md` §Plans ("plan name + price + feature list + Badge + CTA", "recommended/selected" state) never matched either version — stale.

### New on e& — page `25915:75766`
- Page "   ↳ New on e&", no flag; section `New on e&` 29918:4274 labels set `new-on` **26523:22852** as "Final component": `selected=yes` 26523:22851 / `selected=no` 26523:22853, both **170×300**. Atom `.new-on-core` **26523:22827**.
- Exact render (design-context on the set): a **story tile**, not a boxed card — no card surface, no resting border. Image frame 170w, radius `border-radius/7` **24**, inner container radius `border-radius/6` 20 with 4px padding; caption below (gap `spacing/md` 12): `body/md` Regular 14, centered, `text/default/default`, 2 lines. `selected=yes` adds a **2px outline** (`border/lg`; screenshot shows a red→green gradient ring; code-fallback color `light-green/base` #47cb6c).
- "New card" badge: Offers **md** (h `scale/20`, min-w 56, `badge/md` 12), surface **`badge/surface/offers/new-card` #0e2916** (dark green) + white text, top-left ~10-12px inset.
- Statesheet rules (29918:4575/4580/4585, 4640): outline **only on the last card in the carousel**; "Tag and gradient appears only for 3 days"; caption limit "2 lines… Max 34 characters with spaces"; "New badge only appears on the last card with outline". Preview frame also shows a 170×256 in-context story with a "Story animation" overlay component (prototype-only).
- **Code drift:** `NewCard` renders a General-style boxed card (`surface.raised.default`, 1px `border.solid.subtle`, radius 16, overflow hidden) with a 140px grey `imageBox` (r12) and a left-aligned `title.sm` in `md` padding, default w 200 — V1.1 has no box at all; missing `selected` outline state, New-card badge, centered `body.md` caption, 170×300 dims, r24/20 image geometry.
- `new-on-eand.md` anatomy — variants (`selected` no/yes), dims 170×300, part `.new-on-core` — all current. `design.md` §New on e& ("media + title + optional CTA", states: default) — no CTA exists in Figma and the selected state is missing — stale.

### Service 🟡 — page `26019:79144`
- Page name "   ↳ Service 🟡" — **WIP flag** (audit before the flag drops; re-verify later). Section `Service Card` **27474:45670** ("Original component" header); page also carries an `example` section 26977:89142 (Category tile 109×148) and `Final layouts` 27207:91684.
- **Structure (four levels):**
  1. Atom `.service-card-core` **26592:14455** (109×148).
  2. Cell sizes set `.service-card-sizes` **27216:41370**: `size=grid` 27216:41369 **109×148** / `size=carousel` 27216:41368 **128×148**.
  3. Row arranger set `service-card` **28165:35653** (published as "Service card"): `Grid|Carousel × Rows` — grid Single 28165:35734 **335×148**, Two 28165:35786 **335×300**, Three 28165:36597 **335×452**; carousel Single 28165:35652 **392×148**, Two 28165:35654 **392×300**. 5 of 6 combos published — **no 3-row carousel** (mirrors General card's missing combo). Cell pitch: 109×3+2×4 = 335 / 128×3+2×4 = 392 → **4px gap**, same as General.
  4. Section-level set `service-card-section` **27474:94804**: `Layout` = 1-row grid 27474:94801 367×244 / 2-row grid 27474:94802 367×396 / 3-row grid 27474:94800 367×548 / 1-row carousel 27474:94799 367×244 / 2-row carousel 27474:94803 367×396 — a full Section wrapper (64px header + 16px padded body slot). No other card page publishes a section-level set — new pattern (changelog 20.05.26: "Created a service card section component set"; 20.05.26: "Added new service-card size for carousel section"; 02.06.26: docs + statesheet updated — all Ryan).
- Exact render (design-context on 27216:41370): white fill (renders **unbound** `bg-white` — no surface atom / variable binding, unlike deals-card's `.default-surface` — flag to design), h bound `section/body/height/row-2` = **148**, radius **`border-radius/6` = 20**, **no border**; padding pt `spacing/sm` 8, px `spacing/sm` 8, pb `spacing/lg` 16. Icon block: flex-fill, centered, pt `spacing/2xl` 24, **48px circular** `icon-size` instance (r `border-radius/8` 1200, category=3d-icon → `4xl` 48) holding a 3-D illustration bitmap. Label: `typography/title/xs` Suisse Int'l SemiBold **14** lh 1.3, centered, fixed h40 (2 lines), `text/default/default` #191329.
- Badge: Offers **md** "New" — h `scale/20` 20, min-w 56, px8/py4, r8, `badge/md` 12 — surface **`badge/surface/offers/new-plan` #0e2916** (dark green; same hex family as New-on's `new-card`, different token key), absolute **top-left 8px/8px**.
- Statesheet (28342:44141) rules: Variants = Grid | Carousel. **Content configurations ×4** — 1|2-line label × with|without tag — "based on what data is available at runtime. These are not designer choices per tile — the UI adapts to what the backend returns"; "**Badge placement will always be aligned top-left with 8px padding from top and left**". **Badge status:** "Status tags are live data badges. They reflect the user's real-time service state" — all **12 Offers types** shown (New Plan, Sold Out, Limited Time Offer, Green Friday, Mega Deals, Discount, Limited Stock, Best Seller, Online Exclusive, Exclusive for Emirati, Valid for 2 Days, New Card). Anatomy parts: Icon container / Status tag / Label.
- **Internal Figma inconsistencies to flag:** (a) statesheet header strip reads "**Quick tasks**" — copy-paste from another page (same class of error as Plans' "Buttons" section); (b) Badge-status blurb says tags "sit in the **top-right** corner of the icon container" while the content-config rule and the published symbols put the badge **top-left 8px** — designer ruling needed (symbols + explicit rule favor top-left).
- **Code drift:** `ServiceCard` (cards.tsx:108-120) nails the badge position (absolute top 8 / left 8 ✓) and the centered icon-over-label anatomy, but: radius **16 vs 20**; adds a 1px `border.solid.subtle` (Figma has none); `minHeight 116` hug-width vs fixed **148** × 109/128 (no `size` axis); padding `md` 12 uniform vs 8/8/16; icon = 34px text span defaulting to a '◍' glyph vs 48px circular 3-D illustration slot (violates design.md's own "never emoji-substitute an icon" rule); label `body.sm` 12 vs `title/xs` SemiBold 14 2-line; `badge` is a free ReactNode with no tie to the 12 live-data Offers statuses; no row arranger or section-level layout counterpart.
- `service.md` anatomy — cell description (3D illustration icon centered + centered bold label + optional New badge dark green top-left + grid examples) is current, but "**rounded 16**" contradicts Figma's r20, and it records no dims (109/128×148), no `size` axis, and neither the row arranger nor the section set → minor STALE-ANATOMY. `design.md` §Service ("icon + label (+ optional chevron)", states default·**disabled**) — no chevron and no disabled state exist in V1.1 — stale.

### Recommendation — no node (TBD since V1.0)
- **Search method + results (2026-07-04):** (1) `get_metadata` page listing on `pzm63BTLfPfT1stcF89ILQ` — the MCP returns only the "🖼 Cover" page for this file (tried twice; large-file/dynamic-page limitation — all other pages in this audit were reached by direct node-id), so no page named "Recommendation" could be confirmed *or* ruled out by listing alone. (2) `search_design_system` over the **published V1.1 library** for "recommendation" — zero components/styles/variables with that name; the semantic nearest-match is `action-card`/`Action card` (Quick-tasks-family sets, unrelated anatomy). (3) A full "card" sweep of the published library (20 hits: card ×4, Card label, Product cards grid, Action Card (TBD), Link your card, Offers Badges, bill action card, Action card, Text offer card, Insurance card, Card Carousel, deals-card, SIM card, Selection cards, Plan card, Stacked card, Service card) — **no Recommendation card under any alias**. (4) Repo: no code export, no `tools/anatomy/recommendation.md`; `AUDIT.md` §07 row reads "Recommendation | TBD"; design.md's only mention is the screen-mapping line "carousel of Product / Deals for you / New on e& / **Recommendation** / Plans cards".
- **Disposition:** absent on both sides — not MISSING-IN-CODE (there is no Figma spec to miss) and not REMOVED-IN-FIGMA (it never shipped in V1.0 either). Recorded as **TBD / not auditable**; excluded from the drift tally.
- **Watch items:** if a "Recommendation" page appears in a later drop it will likely slot into the card-carousel family (fixed-width tile, per design.md's mapping row) — audit then; alternatively ask design whether the concept was absorbed into Deals-for-you (its like/dislike feedback pill is recommendation-ish) or the `Action card (TBD)` sets. **Do not build speculatively.**
