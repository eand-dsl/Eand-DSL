---
title: Typography
description: The Suisse Int'l type ramp — display, heading, title, body, button.
---

Sizes, weights, and line-heights come from the tokens. The `aed/*` ramp is the
Arabic/RTL parallel — same steps, tuned metrics. Load the licensed Suisse Int'l
font in your app; the stack falls back to `"Helvetica Neue", Inter, system-ui`.

<TokenTypography />
